import React from 'react';
import styled from 'styled-components';

const Image = (props) => {

    return (
        <Text>{props}</Text>
    )
}

const Text = styled.p`
    font-size: 20px;
`;
export default Image;